server:
  port: 8082
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/finpay_db
    username: ${DB_USERNAME:finpay_user}
    password: ${DB_PASSWORD:finpay_pass}
  jpa:
    hibernate:
      ddl-auto: update
